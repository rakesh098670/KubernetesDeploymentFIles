apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: state-pvc
  labels:
    app: nginx
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

